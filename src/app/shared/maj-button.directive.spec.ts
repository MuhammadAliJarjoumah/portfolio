import { MajButtonDirective } from './maj-button.directive';

describe('MajButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MajButtonDirective();
    expect(directive).toBeTruthy();
  });
});
